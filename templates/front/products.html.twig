{% extends "front/base.html.twig" %}

{% block main %}


	{% for product in products %}
	{#<img src="{{ asset('image/' ~ product.images[0].src) }}" alt="{{ product.images[0].alt }}">#}
	
		<h2>
			<a href="{{ path("product_show", {'id' : product.id } ) }}">{{ product.name }}</a>
		</h2>
	{% endfor %}

{% endblock %}