{% extends "admin/base.html.twig" %}

{% block main %}

	<h1>{{ admin }}</h1>

	{% for image in <product class="image"></product> %}

		<img src="{{ asset('image/' ~ image.src) }}" alt="{{ image.alt }}">

	{% endfor %}

	<h2>{{ product.name }}</h2>
	<h3>{{ product.price }}</h3>

{% endblock %}